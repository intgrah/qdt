import Std.Eq

inductive Unit where
  | unit

def Unit.eta (u : Unit) : () = u :=
  Eq.refl Unit u

def Unit.ext (a b : Unit) : a = b :=
  Eq.trans Unit a Unit.unit b (Eq.symm Unit Unit.unit a (Unit.eta a)) (Unit.eta b)
