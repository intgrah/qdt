import Std.Init

inductive False where

def False.elim (A : Type) (e : False) : A := False.rec (fun _ => A) e

def Not (A : Type) := A → False

def absurd (A B : Type) (ha : A) (hna : Not A) : B := False.elim B (hna ha)

def not_false : Not False := id False
