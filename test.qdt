import Std

structure Foo (A B : Type) where
  (fst : A)
  (snd : B)

example : 1 + 1 = 3 - 1 := Eq.refl Nat 2

def prod_swap (A B : Type) (p : Prod A B) : Prod B A :=
  Prod.mk B A (Prod.snd A B p) (Prod.fst A B p)

def sigma_snd := Sigma.snd
