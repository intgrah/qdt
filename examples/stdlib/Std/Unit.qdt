import Std.Eq

inductive Unit where
  | unit

def Unit.eta (t : Unit) : () = t :=
  Eq.refl.{0} Unit ()

def Unit.ext (a b : Unit) : a = b :=
  Eq.trans.{0} Unit a Unit.unit b (Eq.symm.{0} Unit Unit.unit a (Unit.eta a)) (Unit.eta b)
