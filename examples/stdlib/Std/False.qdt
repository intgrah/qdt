import Std.Init

inductive False where

def False.elim.{u} (A : Type u) (e : False) : A := False.rec.{u} (fun _ => A) e

def Not (A : Type) := A → False

def absurd.{u} (A : Type) (B : Type u) (ha : A) (hna : Not A) : B := False.elim.{u} B (hna ha)

def not_false : Not False := id.{0} False
