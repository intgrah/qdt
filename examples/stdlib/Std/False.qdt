import Std.Init

inductive False where

def False.elim.{u} (α : Type u) (e : False) : α := False.rec.{u} (fun _ => α) e

def Not (α : Type) := α → False

def absurd.{u} (α : Type) (β : Type u) (ha : α) (hna : Not α) : β := False.elim.{u} β (hna ha)

def not_false : Not False := id.{0} False
