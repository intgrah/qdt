import Std.Eq

structure Sigma.{u, v} {α : Type u} (β : α → Type v) : Type (max u v) where
  (fst : α)
  (snd : β fst)

def Sigma.eta.{u, v}
    {α : Type u} {β : α → Type v} (p : Sigma β) :
    Sigma.mk (Sigma.fst p) (Sigma.snd p) = p :=
  rfl
